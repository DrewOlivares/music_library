// create context
    //createContext
//provide context
    //context.Provider with value
//consume context
    //useContext(Context)
import { createContext } from "react"

export const ThemeContext = createContext({})